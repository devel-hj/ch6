# 더하기 함수 만들기
# def add(a, b):
#     return a + b

# 람다 함수(익명 함수) 만들기
# 변수 = lambda 매개변수 : 반환값
add = lambda a, b: a + b

# 람다 함수 호출
result = add(3, 4)
print(result)

# 꼭 써야하는가? x
# 복합 대입, 람다함수는 선택
sum += 1